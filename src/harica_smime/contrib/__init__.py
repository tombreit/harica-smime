"""Optional extras that are not part of the core HARICA client.

Anything under :mod:`harica_smime.contrib` is add-on tooling: the CLI,
staging smoke tests, and browser-side static assets. The core library —
:class:`harica_smime.Client` and its dependencies — does not import from
this package.
"""
